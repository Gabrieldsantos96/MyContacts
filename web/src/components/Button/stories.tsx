import { Meta, StoryObj } from '@storybook/react'
import Button from '.'

export default {
  title: 'Components/Button',
  component: Button
} as Meta

export const Default: StoryObj = {}

Default.args = {}
